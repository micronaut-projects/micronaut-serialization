Task 10 evidence: Bean deserializer optimization happy path

Command:
./gradlew :micronaut-serde-processor:compileJava :micronaut-serde-jackson:test --tests 'io.micronaut.serde.jackson.annotation.SerdeSourceGenBeanSpec'

Result:
- BUILD SUCCESSFUL
- `SerdeSourceGenBeanSpec` executed and passed (`test default-constructor bean sourcegen serializer and deserializer are concrete and functional`).

Supplemental command:
./gradlew :micronaut-serde-jackson:test --tests 'io.micronaut.serde.jackson.annotation.SerdeRuntimeSelectionSpec'

Supplemental result:
- BUILD SUCCESSFUL
- `SerdeRuntimeSelectionSpec` executed and passed (covers runtime `findDeserializer(...).createSpecific(...)` selection path).
