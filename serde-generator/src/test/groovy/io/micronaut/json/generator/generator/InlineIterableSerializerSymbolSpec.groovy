package io.micronaut.json.generator.generator

class InlineIterableSerializerSymbolSpec extends AbstractSymbolSpec {
    // can't test here because we can't create generic ClassElements :(
    // --> tests in MapperVisitorSpec
}
